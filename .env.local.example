# Required: Perplexity API key (server-side only, no BYOK)
PERPLEXITY_API_KEY=your_api_key_here

# Required: Base URL for shareable links
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Required: Vercel Blob storage token for manual storage
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Optional: Admin secret for /api/admin/feedback endpoint
# Falls back to CRON_SECRET, then "dev-secret" in development
# ADMIN_SECRET=your_secret_key_here

# Optional: Cron secret for /api/cron/* endpoints (Vercel Cron)
# CRON_SECRET=your_cron_secret_here
